// blueprints_test.go

package processors

import (
	"testing"
)

func TestGetBlueprintsLocation(t *testing.T) {
	// Test with local blueprints
	//localPath, err := GetBlueprintsLocation(false)
	//assert.NoError(t, err)
	//assert.Equal(t, "/path/to/local/blueprints", localPath)

	// Test with remote Git repository
	//localPath, err = GetBlueprintsLocation(true)
	//assert.NoError(t, err)
	//assert.Equal(t, "/path/to/local/blueprints", localPath)
	// Add more assertions to check if the Git repository was cloned correctly
}
