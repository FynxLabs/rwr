blueprints:
  format: yaml
  location: '.'
  order:
    - bootstrap
    - packages
    - files
    - services
    - git
    - scripts
    - ssh_keys
packageManagers:
  - name: cargo
    action: install
  - name: chocolatey
    action: install