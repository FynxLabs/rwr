blueprints:
  format: yaml
  location: .
  order:
    - packages
    - git
    - files
    - scripts

variables:
  userDefined:
    project_name: "minimal-setup"
    repo_url: "https://github.com/user/dotfiles.git"
