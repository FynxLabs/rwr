## Actions Available
# - install
# - remove

packages:
  - names:
      - git
      - jq
    action: install
    package_manager: apt

  - name: bitwarden-cli
    action: install
    package_manager: brew